// This file is no longer needed - setup.ts contains the global hooks
// import { afterAll, beforeAll } from 'vitest';
// import { cleanupWorkspace, setupWorkspace } from './shared/setup';

// // Global setup - runs once before all tests
// beforeAll(async () => {
//   console.log('🌍 Global setup: Preparing workspace for all smoke tests...');
//   await setupWorkspace();
// }, 300000); // 5 minutes timeout

// // Global teardown - runs once after all tests
// afterAll(async () => {
//   console.log('🧹 Global cleanup: Cleaning up workspace...');
//   await cleanupWorkspace();
// }, 60000); // 1 minute timeout
